VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GEMINI_API_KEY=

# ElevenLabs (server-side only — no VITE_ prefix so the API key never reaches the browser).
# Used by /api/elevenlabs/tts.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
